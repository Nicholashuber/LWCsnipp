const tabItems = {
    components: {
        title: 'Components',
        value: 'components',
        visible: false
    }
};

const tabElements = ['components', 'lockersafejs', 'learning'];

export { tabItems, tabElements };
